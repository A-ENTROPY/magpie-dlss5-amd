# 0.6.8 Draft 分发准备

维护者已明确授权：将 `068` 合并回此前开发主线，推送源码，参照 067 准备分发内容并创建 GitHub Draft；暂不发布，由维护者人工审核 Release note。本轮授权包含文件准备、源码推送、Draft 创建与附件上传，无需再次请求这些步骤的许可。

## 源码与版本

- 刷新远端后，GitHub 默认分支及此前开发主线均为 `experimental`，远端提交 `7eb19987e73ad2e65bc7d75056aece5465a10204` 是 `068` 的祖先；快进合并到 `4c644f0df9524320fb5c87fbb5c6a4fa1302d1cd`，无冲突。`068` 分支保留，发布文档和打包整合提交追加在主线上。
- 程序／包内清单版本使用 `0.6.8`，计划标签 `v0.6.8-experimental`，标题 `Magpie Experimental v0.6.8`。最终提交由分发清单与 GitHub Draft 核对，不在此提前写入尚未产生的提交 SHA。
- Draft 尚不可公开下载，因此遵循 Git 工作流对公开版本入口的规定，`version.json` 保持 0.6.7；发布时再更新入口，不把推送源码等同于发布新版本。
- 067 线上 Release 实际标签为 `untagged-1a7d59dfa5a6fd93c888`。不修改这份既有 Release、附件或标签。

## 分发内容

分发目录：工作区下 `release/v0.6.8-experimental/`。仅以下三个文件作为 GitHub 附件，与当前 067 一致：

| 文件 | 来源 |
| --- | --- |
| `Magpie-Experimental-x64.zip` | 从干净主线提交执行 `scripts/Build-Release.ps1` 完整 Release x64 构建，版本 0.6.8 |
| `DLSSNR-DLL-Options-310.8.0.0.zip` | 原样复用线上 067 对应的本地附件，三种 DLL 及中英说明；SHA-256 `2AB29107A782733B310D62D5D8C4ABCEE7185B853386198FF08D6AA751A5DCEC` |
| `NGX_OTA_Switch.bat` | 原样复用 067；SHA-256 `D0F96B57F7E91101460EEB68CD618B2C11866A4DFC6901741C17ACECAC04C465`；仅打包，不执行 |

主 ZIP 解压后顶层为 `Magpie-Experimental-x64/`。包内包含 README、更新说明、第三方声明、GPLv3／厂商许可证、构建清单，并补入 DLSSNR Multi Pass、XeSSFG 指南及兼容源码声明。调试符号、obj、审计文件与本机配置不上传。068 local 保持现有运行部署，正式分发输出独立于 local。

为新版本打包时，`-RequireMagpieClosed` 只要求目标包 EXE 未被占用，不终止另一个目录内正在运行的 Magpie。正式打包脚本同时保护 067 local 与 068 local，禁止作为可清空的分发目标。

## 说明与验证边界

- Release note 只写 068 新功能、使用方法及影响用户的已知限制；开发过程与测试证据放在此处和已有技术文档，不写入 Release note。
- 功能源自当前已部署并完成专项回归的 068：Multi Pass、四条时域路线、统一 XeSSFG、多帧兼容／慢启动修正、重复帧开关、截图时间戳修正与参数焦点入口调整。
- 合并后的针对性回归、构建、EXE／PRI、效果元数据、清单、ZIP、可选附件和上传后的名称／字节数／SHA-256，以分发目录内 `LOCAL-PREPARATION-AUDIT.json`、`DISTRIBUTION-MANIFEST.json`、`GITHUB-DRAFT-VERIFICATION.json` 的实际结果为准。
- CPU／WARP／SDK 用例不证明游戏画质和显示帧节奏。RTSS 重复切换时的初始化崩溃仍未解决；四路线实机截图对照及 HDR／应用组合仍待确认。保留在更新说明的已知限制中。
- 厂商条款及 GPL 兼容性仍遵循现有第三方声明，不能把旧附件复用或打包通过当成已完成公开再分发审核。本轮仅创建 `draft: true`、`prerelease: true`；发布需后续明确指令。
